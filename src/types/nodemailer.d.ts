declare module "nodemailer" {
  // Typage minimal pour satisfaire TypeScript dans ce projet.
  // Si besoin, on pourra affiner plus tard avec les vraies signatures.
  const nodemailer: any;
  export default nodemailer;
}

